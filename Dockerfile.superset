FROM apache/superset:3.1.0

USER root

# snowflake-sqlalchemy is needed for the Snowflake database connection in Superset.
# It depends on snowflake-connector-python which is pulled in automatically.
RUN pip install --no-cache-dir \
    snowflake-sqlalchemy==1.5.1

USER superset
