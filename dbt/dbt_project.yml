name: retail_sales
version: "1.0"
config-version: 2
profile: default

# Directories dbt uses for each resource type (defaults shown explicitly for clarity)
model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  retail_sales:
    +materialized: incremental
